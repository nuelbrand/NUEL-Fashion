/**
 * PROFILE PAGE
 * ============
 * FILE: src/pages/account/ProfilePage.jsx
 * URL:  /account/profile
 *
 * Protected — only accessible when logged in.
 * ProtectedRoute in the router handles the redirect to /login
 * if the user is not authenticated.
 *
 * Re-exports from SupportingPages for clean file structure.
 */

export { ProfilePage as default } from '../SupportingPages'
