/**
 * POSTCSS CONFIGURATION
 * =====================
 * PostCSS is a tool that processes your CSS.
 * Tailwind uses it to generate all the utility classes.
 * Autoprefixer adds browser compatibility prefixes automatically.
 *
 * You do not need to touch this file — it just needs to exist.
 */

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
